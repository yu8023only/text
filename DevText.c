#include<stdio.h>
int main(){
	//比较大小
	/*int a, b, c;
	printf("请输入三个大小不同的数：");
	scanf("%d %d %d", &a, &b, &c);
	
	int max = 0;
	if( a>b ){
		if( a>c ){
			max = a;
		}else{
			max = c;
		}
	}else{
		if( b>c ){
			max = b;
		}else{
			max = c;
		}
	}
	printf("这三个整数中最大的是%d\n", max);*/
	
	printf("* * * *\n");
	printf(" * * *\n");
	printf("  * *\n");
	printf("   *\n");
	
	return 0;
}